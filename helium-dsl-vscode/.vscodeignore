# Exclude parent directory files (including workspace node_modules)
../**
# Explicitly exclude workspace node_modules to prevent vsce from following symlinks
../node_modules/**

# Exclude source files
src/**
tsconfig.json
tsconfig.tsbuildinfo

# Exclude development files
scripts/**
*.md
.gitignore
.cursorrules

# Include node_modules for extension dependencies (vscode-languageclient)
# vsce will bundle dependencies from package.json automatically when they are properly installed
# We only need to exclude nested node_modules to prevent duplicates
# DO NOT exclude node_modules/** - vsce needs to include dependencies
# IMPORTANT: server/node_modules/ must be included for the language server to work
# Exclude nested node_modules directories to prevent duplicate file errors
# This pattern excludes node_modules/package/node_modules/ but allows server/node_modules/
**/node_modules/*/node_modules/**

# Exclude build artifacts
*.vsix

# Exclude language server source (only need compiled output)
../helium-dsl-language-server/**

# Exclude generated files in parent (only need copied ones)
../generated/**

# Exclude scripts in parent
../scripts/**

# Exclude other root files
../validate-dsl.sh
../package.json
../package-lock.json
../tsconfig.json
../test-munic-chat.js
../parser-warnings-example.md
../old_rules.md
../README.md
../LICENSE
../.gitignore
../.cursorrules
../.git/**
